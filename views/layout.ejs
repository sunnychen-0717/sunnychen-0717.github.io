<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= typeof title !== 'undefined' ? title : 'BookApp' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/styles.css">
  <style>
    .btn { padding:.5rem .75rem; border:1px solid #ccc; border-radius:6px; text-decoration:none; }
    .btn-primary { background:#2563eb; color:#fff; border-color:#2563eb; }
    nav a { margin-right:.75rem; }
  </style>
</head>
<body>
  <nav style="display:flex;align-items:center;justify-content:space-between;padding:1rem;border-bottom:1px solid #eee;">
    <div>
      <a href="/">Home</a>
      <% if (username) { %>
        <a href="/books">Books</a>
        <a href="/books/new" class="btn btn-primary">+ Add Book</a>
      <% } %>
    </div>
    <div>
      <% if (username) { %>
        <span>Signed in as <strong><%= username %></strong></span>
        <a href="/logout" style="margin-left:.5rem;">Log out</a>
      <% } else { %>
        <a href="/login">Log in</a>
        <a href="/signup" class="btn btn-primary">Sign up</a>
      <% } %>
    </div>
  </nav>

  <main style="max-width:900px;margin:0 auto;padding:1rem;">
    <%- body %>
  </main>

  <footer style="max-width:900px;margin:2rem auto 1rem;padding:1rem;color:#666;border-top:1px solid #eee;">
    © 2025 BookApp
  </footer>
</body>
</html>
